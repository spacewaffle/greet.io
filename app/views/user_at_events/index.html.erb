<h1>Listing user_at_events</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_at_events.each do |user_at_event| %>
  <tr>
    <td><%= user_at_event.name %></td>
    <td><%= user_at_event.content %></td>
    <td><%= link_to 'Show', user_at_event %></td>
    <td><%= link_to 'Edit', edit_user_at_event_path(user_at_event) %></td>
    <td><%= link_to 'Destroy', user_at_event, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User at event', new_user_at_event_path %>
