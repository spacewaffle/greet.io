<%= form_for(@user_at_event) do |f| %>
  <% if @user_at_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_at_event.errors.count, "error") %> prohibited this user_at_event from being saved:</h2>

      <ul>
      <% @user_at_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
